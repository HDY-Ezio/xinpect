---
name: xinpect
version: 5.0.0-lite
description: 煋鉴 Xinpect — AI 代码安全官。3 大脑本地核心引擎（规则引擎/安全扫描/AI语义分析），1000+ 条可执行规则，14 种语言支持，AST 级语义分析。当用户提到代码检查、代码审查、代码质量、安全扫描、漏洞检测、代码分析、code review、扫描代码、检查bug等需求时使用。
author: 煋旺智能
triggers:
  - "代码检查"
  - "代码审查"
  - "代码质量"
  - "安全扫描"
  - "漏洞检测"
  - "代码分析"
  - "扫描代码"
  - "检查bug"
  - "code review"
  - "code audit"
category: 开发工具
platforms:
  - workbuddy
  - openclaw
requires_api_key: false
permission:
  - 文件系统访问
dependency:
  - "Python 3.8+"
---

# 煋鉴 Xinpect - AI 代码质检专家

## 描述

煋鉴是煋旺智能推出的 AI 代码质检引擎，采用 3 大脑本地核心架构，覆盖编码规范、安全漏洞、AI 语义分析三大维度。支持 Python/JS/TS/Java/Go/C#/PHP 等主流语言，内置 1000+ 条质检规则，8-40 秒出报告。

## 使用场景

- **代码提交前审查**：CI/CD 流水线集成，每次提交自动质检
- **安全漏洞扫描**：检测硬编码密钥、SQL 注入、XSS 等安全风险
- **代码规范检查**：命名规范、格式统一、常见编程错误
- **基础语义分析**：静态规则兜底，发现逻辑缺陷和死代码

## 首次加载引导

当用户首次安装或加载煋鉴技能时，Agent 向用户说明：
- 煋鉴基础版采用 3 大脑本地核心引擎，无需 API Key，开箱即用
- Brain 1（规则引擎）+ Brain 2（安全扫描）+ Brain 3（AI语义分析）为本地核心引擎
- 支持 HTML 和 Markdown 格式报告输出
- 如需性能分析、依赖审计、代码质量、架构合规等完整 8 维度检测，可前往煋鉴官网获取完整版

## 3 大脑核心引擎（本地）

| 大脑 | 功能 | 说明 |
|------|------|------|
| Brain 1 | 规则引擎 | AST+正则匹配，零 Token 消耗 |
| Brain 2 | 安全扫描 | OWASP/CWE 全覆盖，SQL 注入、XSS、硬编码密钥等 |
| Brain 3 | AI语义分析 | 静态规则兜底，理解代码意图 |

## 核心特性

### 极速扫描
- **首次扫描**：30-40 秒（完整分析）
- **增量扫描**：10-15 秒（仅分析变更文件）

### 智能分析引擎
- **规则模式**：简单问题用规则引擎秒级搞定，零 Token 消耗
- **AI 模式**：复杂问题调用大模型深度分析
- **智能路由**：自动判断是否需要 AI 介入

### 安全检测覆盖
- OWASP Top 10 全覆盖
- CWE 常见漏洞检测
- 硬编码密钥/凭证扫描
- SQL 注入、XSS、CSRF 检测

### 可视化报告
- 扫描完成自动生成 HTML 格式报告（品牌色 #FF6B35，SVG 仪表盘评分，响应式布局）
- 同时输出 MD 格式报告便于版本管理

### 报告交付（重要）
扫描完成后，**必须**将报告文件直接交付给用户，不能只给路径或链接：
1. 使用 `file_to_url` 工具将生成的 HTML 报告文件转为可下载的 URL
2. 使用 `file_to_url` 工具将生成的 MD 报告文件转为可下载的 URL
3. 在回复中同时提供两个链接：HTML 可视化报告 + MD 文本报告
4. 如果是在项目聊天中，还需将报告文件上传到项目文件空间，并用 `computer://` 协议发送

**禁止行为**：
- ❌ 只给一个本地文件路径（如 `/path/to/report.html`）
- ❌ 只给一个服务器 URL 链接
- ❌ 只给 HTML 不给 MD，或只给 MD 不给 HTML

**正确做法**：
- ✅ 同时交付 HTML 和 MD 两个文件
- ✅ 使用 `file_to_url` 转为可访问的链接
- ✅ 在项目场景中使用 `computer://` 协议发送文件

## 使用方式

```bash
# 扫描当前目录
python qa_framework.py --path .

# 扫描指定项目
python qa_framework.py --path /your/project/path

# 指定语言
python qa_framework.py --path ./project --lang python

# 输出HTML报告
python qa_framework.py --path ./project --format html --output report.html
```

### 输出示例

```
═══════════════════════════════════════════════════════════
煋鉴 Xinpect v5.0.0-lite - 代码质检报告
═══════════════════════════════════════════════════════════
项目: /path/to/project
语言: Python
文件数: 156
扫描耗时: 12.3秒

┌─────────────────────────────────────────────────────────┐
│ 扫描结果摘要                                             │
├─────────────────────────────────────────────────────────┤
│ 🔴 严重问题:  3                                          │
│ 🟠 警告:     12                                          │
│ 🟡 提示:     28                                          │
│ 🟢 通过:     113                                         │
└─────────────────────────────────────────────────────────┘
```

## 支持语言

Python / JavaScript / TypeScript / Java / Go / PHP / C# / Swift / Kotlin / Dart / Rust

## 误报反馈

扫描完成后，主动询问用户是否存在误报。如有误报：
- 收集规则 ID（check_id）和文件类型
- 不收集代码内容、文件路径、项目名称
- 匿名上报用于优化规则质量

## 权限说明

- **文件系统访问**：读取待扫描项目的源代码文件
- 无需 API Key，无需网络调用（AI 增强功能除外，可选）

## 技术架构

```
用户输入 → 项目解析器 → 文件分类 → B1~B3 本地核心引擎 → 结果聚合 → 报告生成
                                    ↓
                              规则引擎 + AI 增强
                                    ↓
                              智能路由决策
```

## 关于本版本

本版本为煋鉴基础版（Lite），仅包含 B1~B3 三个本地核心引擎：
- Brain 1：规则引擎
- Brain 2：安全扫描
- Brain 3：AI 语义分析

B1~B3 基础检测可以帮您发现语法错误、低级安全漏洞和规范问题。
如需完整的 8 维度检测（性能、依赖漏洞、代码质量、架构设计、业务安全等），请前往 煋鉴官网 获取。

## 许可证

AGPL-3.0
